// UNUSED — the office calibration feature was reverted at user request.
// File kept (can't be deleted via the file tool) but no code requires it,
// so the schema is never registered and the collection is never touched.
module.exports = null;
